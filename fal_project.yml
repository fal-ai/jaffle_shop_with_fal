runtime:
  api: "{{ env_var('FAL_RUNTIME_URL') }}"
  bucket: "{{ env_var('FAL_JOBS_BUCKET') }}"
environments:
  - name: prophet
    requirements:
      - prophet==1.1.1
  - name: kmodes
    requirements:
      - kmodes==0.12.1
